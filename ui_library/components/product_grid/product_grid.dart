import 'package:flutter/material.dart';

class ProductGridComponent extends StatelessWidget {
  const ProductGridComponent({super.key});
  @override
  Widget build(BuildContext context) => const Placeholder();
}
